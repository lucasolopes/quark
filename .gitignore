/target
# clippy roda com CARGO_TARGET_DIR proprio: misturar artefatos de check (rmeta)
# com os de build (rlib) no mesmo target/ corrompe a compilacao dos testes.
/target-clippy
*.mdb
/data

# frontend
node_modules/
web/dist/
web/node_modules/
*.log

# Wrangler local cache (account id, project cache) — nunca commitar
.wrangler/

# local e2e test artifacts (not committed)
.playwright-mcp/
modal-*.png
page-*.png
web/.env.local
/*.png

# Artefatos de processo SDD (briefs, reports, diffs de review) — scratch local, fora do repo.
.superpowers/

# Claude Code: worktrees e estado local ficam fora do repo.
# .claude/skills/ é versionada de propósito (convenções do projeto).
.claude/worktrees/
.claude/settings.local.json
.claude/scheduled_tasks.lock
